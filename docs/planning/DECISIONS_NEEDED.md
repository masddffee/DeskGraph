# Decisions Needed

Last reviewed: 2026-07-19

## Blocking now

D-006 is resolved at the architecture level by ADR-033: one explicitly confirmed Coverage Set minus hard exclusions. Bounded atomic multi-root authorization is implemented locally; hard exclusions and the associated privacy purge still await implementation. D-008 is resolved at the architecture level by ADR-024: native-first with a separately gated packaged fallback. D-015 now owns the fallback implementation choice. D-013 must be resolved before M4 introduces cross-root learned scoring; D-016 separately gates merge/split identity. ADR-028 resolves the first suggest-only multi-member screenshot-review schema/rule without claiming similarity or disposability; broader screenshot-origin evidence and group correction still require separate evaluation. ADR-029 resolves only the path-free Smart Cleanup Inbox read-model boundary and keeps relation feedback separate from cleanup consent. ADR-030 accepts an independent, path-free, non-executable durable Preview core with complete target/keeper evidence binding. ADR-031 now accepts only an explicit Desktop review that transiently reveals current authorized member paths and creates the same non-authorizing Preview through ID-only IPC; it adds no confirmation or filesystem action. D-014 must be resolved before splitting the long-running writer into a standalone OS service, but does not block an independent read-only stdio MCP process. D-017 must be accepted before Smart Cleanup exposes confirmation or an executable system-trash action; it does not block explainable suggest-only candidates, the ADR-029 Inbox, ADR-030 core Preview, or ADR-031 explicit local review. ADR-026 resolves D-018 by keeping general Unix Rename/Move Preview-only. ADR-027 resolves D-019's identity-first architecture and keeps the Tauri Rust core as the only v0.1 action host; Windows selects a protected private-namespace mutex contract, while the macOS `flock` candidate still depends on D-002's OS floor and signed protected-container replacement proof. M9a now provides only a local macOS native-picker/security-scoped-bookmark foundation; D-002, D-003 and D-010 plus signed clean-machine acceptance still block a platform-support claim. Windows and Linux release authorization deliberately fail closed until real platform capability adapters exist. GitHub remote ownership blocks only remote CI/Issue/Release evidence.

## Open decisions

| ID    | Decision                                                    | Needed by                             | Options / constraints                                                                        | Default until decided                                                      |
| ----- | ----------------------------------------------------------- | ------------------------------------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| D-001 | GitHub owner and public repository location                 | M0 CI verification                    | Personal vs organization ownership; repository name and default branch protection            | Local Git only; do not invent a remote                                     |
| D-002 | Minimum supported macOS version and Intel strategy          | M9 design, benchmark fixtures earlier | Universal binary vs split arm64/x64; Tauri/WebKit support and clean machines determine floor | Build both architectures in CI design; no support claim yet                |
| D-003 | Windows package identity, installer, and updater format      | M9                                    | Full MSIX plus App Installer/update URI, Microsoft Store MSIX, or an explicitly reviewed sparse/external-location identity design. Package-family stability, certificate subject/publisher identity, repair/update/uninstall behavior and Windows OCR/action-fence compatibility are mandatory. Tauri MSI/NSIS alone does not establish the package identity required by DeskGraph | No Windows release authorization or OCR runtime claim; evaluate full MSIX first and do not infer identity from an MSI/NSIS installer |
| D-004 | Opt-in telemetry and crash reporting                        | Before public beta                    | None, local-only export, or privacy-reviewed opt-in provider                                 | No telemetry and no network egress                                         |
| D-005 | Linux experimental artifact                                 | M9                                    | AppImage first, `.deb` optional; cannot delay macOS/Windows                                  | AppImage candidate, explicitly experimental                                |
| D-007 | Disposable vector acceleration after SQLite embeddings      | M3                                    | Compare bounded exact search with an ANN candidate using real chunk/vector counts, recall@k/result consistency, p95, build/update time, RSS, primary-target packaging, license, atomic model-version invalidation and recovery; indexes rebuild from version-matched SQLite embedding rows | No vector dependency selected; exact-first benchmark and deterministic lexical fallback |
| D-009 | Embedding model/runtime                                     | M3/M9                                 | Multilingual, int8, license, checksum, memory, model-removal support                         | Deterministic lexical retrieval remains required                           |
| D-010 | Product trademark/name clearance and reverse-DNS identifier | Before signed release                 | DeskGraph availability and legal review                                                      | Development identifier only; no trademark claim                            |
| D-013 | Cross-root learned Project scoring governance                | Later M4 scoring and correction slice | Whether/how labels influence different candidates; versioned features/model/calibration corpus; poisoning/reset/rollback; explainable bounded influence | Store append-only labels only; feedback affects the exact stable root; no online learning or cross-root adjustment |
| D-014 | v0.1 long-running writer process topology                    | Before M6 background acceptance or any writer daemon | Tauri core process plus tray/autostart vs per-user daemon; a daemon requires authenticated IPC, ACL/peer identity, protocol/update skew, startup/clean-restart, installer and uninstall evidence; stdio MCP stays independently read-only | Tauri core process owns the durable background-runner lease; no localhost HTTP, writer daemon, socket or named pipe |
| D-015 | Packaged OCR fallback provider                              | M2/M9                                 | The versioned evaluator and macOS Vision runner now establish corpus/image/manifest binding, production-shared byte/output validation, canonical text reconstruction, CER/spatial/latency contract and sensitive evidence handling. Still bake off Tesseract `eng`+`chi_tra` against PP-OCRv6 small/tiny on the same licensed zh-Hant/English corpus with equivalent runner, process topology, RSS/cancellation and checksums/SBOM; macOS arm64/Intel and Windows x64 block adoption, Linux remains experimental/nonblocking | No fallback runtime or router selected; native capability failures remain honest fixed errors |
| D-016 | Project merge/split identity and reversal semantics          | Later M4 graph correction slice        | Stable merged/split identities, membership provenance, reversible append-only events, conflict/current-data behavior and retrieval invalidation | No merge, split or automatic membership |
| D-017 | Cross-platform system-trash receipt, restore, and guarantee boundary | Before confirmation or executable Smart Cleanup in M5/M6/M8 | Official-source review identifies only conditional candidates. macOS: `FileManager.trashItem(at:resultingItemURL:)` plus a security-scoped bookmark created from the returned actual Trash URL; Undo would be a same-volume, destination-absent `moveItem`, but Apple does not document exact-source binding for `trashItem`, rejects file-reference URLs for `moveItem`, and does not guarantee atomic no-replace against a concurrent destination race. Windows: `ITransferSource::RecycleItem` may return the recycled `IShellItem`; a serialized absolute PIDL may be reconstructed and moved with `TSF_NORMAL`, which documents fail-if-exists, but Microsoft does not guarantee Recycle Bin PIDL durability across process/Explorer/session/reboot/OS changes and the source `IShellItem` still lacks a reviewed handle-bound mutation guarantee. Accept only after signed/runtime hostile-replacement, restart, collision, external-empty/change and crash matrices prove the exact supported boundary. Trash remains an action-bound non-enumerable capability; no shelling out, generic delete fallback, overwrite, permanent delete, empty-trash, Finder private metadata or `$Recycle.Bin` format access. Linux remains experimental and non-blocking | Keep Preview-only and `action_authorized: false` / `execution_available: false`. Existing previews are permanently non-confirmable. Do not expose confirmation, Trash, Execute, recovery or Undo until a new immutable execution binding stores the original destination and per-action receipt contract, the platform action is exact-source bound, and every conditional receipt failure becomes `needs_attention` rather than fallback behavior |

## Decisions made in M0

- Version B is the target; Version A is only an internal milestone.
- Project code license: Apache-2.0 (recorded in ADR-008).
- Rust workspace + Tauri 2 + React/TypeScript + pnpm follows the accepted architecture.
- Health data is a closed, privacy-safe schema shared by CLI and desktop; no paths, filenames, content, identifiers, or model output.

## Decisions made in M1

- ADR-033 resolves D-006 at the architecture level: authorization is one explicit Coverage Set, not a product requirement to repeat one-folder onboarding. The effective set is active confirmed roots minus active hard exclusions. Home is advanced opt-in; default macOS Full Disk Access and Windows `broadFileSystemAccess` are rejected. Durable policy revision and atomic privacy purge are required before exclusions can be called implemented.
- Bundled SQLite through `rusqlite 0.40.1` is the manifest source of truth.
- File identity is separate from location: Unix device/inode and Windows volume serial/file index through an isolated `windows-sys` adapter.
- Unicode comparison keys use NFC; Windows additionally uses case-insensitive comparison keys. Canonical scope validation remains the security boundary.
- Initial scan is metadata-only and never follows symlinks or Windows reparse points.
- Resumable scans use a persistent path queue and job-scoped staging; only a completed job publishes the live manifest atomically. Pause state and an expiring runner lease survive process exit (ADR-011).

## Decisions made while entering M2

- Extractor providers receive only core-controlled bounded streams, never arbitrary paths; output remains untrusted and publishes per file only after complete success (ADR-012).
- Plain text, Markdown, and source code use a dependency-free built-in UTF-8 provider. Invalid encoding is isolated per file rather than silently guessed.
- Extraction jobs and content chunks are durable SQLite state. Failure or cancellation preserves the prior complete extraction; a changed source invalidates stale active chunks.
- Ordinary CLI/Desktop extraction status contains stable IDs, fixed codes, counts, and timings only. Paths and extracted text are excluded.
- D-008 is resolved by ADR-024 at the provider-stack level: macOS uses Apple Vision first; Windows may use `Windows.Media.Ocr` only with package identity and acceptable installed recognizers. Windows requests `zh-TW`/`en-US`, then validates the actual recognizer as Traditional Chinese (never `zh-Hans`) or English. The macOS `objc2-vision 0.3.2` implementation passes local arm64 Manifest→Vision→spatial SQLite/FTS evidence. The existing Microsoft `windows 0.61.3` provider code passes dependency/API/license/RustSec, host policy/state-machine, and Windows cfg check/Clippy gates without adding a lock package; real Windows/MSIX/language/cancellation/cleanup/memory evidence remains open. Missing identity/language currently returns a fixed error—automatic fallback routing is not implemented. D-015 reopens only the packaged fallback implementation: Tesseract and PP-OCRv6 are candidates, not accepted dependencies. `ocrs` remains excluded from v0.1 because it does not meet the Traditional Chinese requirement.
- D-015's first evidence runner is macOS Vision only. It consumes an explicit private asset manifest/root, binds exact corpus/manifest/image bytes, shares production image/OCR validators, reconstructs canonical text under a versioned rule, and writes sensitive no-clobber evidence. Its one-case private synthetic outside-sandbox pass proves harness connectivity, not provider selection, representative quality, packaging, RSS, cancellation, Intel, Windows, or fallback readiness.
- PDF text uses exact `lopdf 0.44.0` with default features disabled, strict bounded in-memory APIs, sequential page processing, no password handling, and no active-content traversal (ADR-013).
- Content-chunk provenance is tagged: source text uses byte ranges; PDF uses page/fragment indexes; DOCX uses paragraph/fragment, PPTX slide/fragment, and XLSX bounded sheet/cell/fragment. Structural formats never receive fabricated byte offsets.
- D-011 is resolved by accepted ADR-014: exact no-default `zip 8.6.0` with only `deflate-flate2-zlib-rs` and no-default `quick-xml 0.41.0` pass isolated closure, license, RustSec, API, macOS arm64, and Windows x64 compile gates. The implemented provider's adversarial, migration, Manifest→SQLite→FTS, full-lock audit, and local release-build gates pass; representative corpora, remote runtimes, live UI, and 8 GB evidence remain release gates, not open dependency choices.
- Image metadata uses exact no-default `imagesize 0.15.0` with only BMP/GIF/JPEG/PNG/TIFF/WebP. It receives a bounded controlled-byte cursor, adds no transitive package, performs no pixel/EXIF/GPS decode, and publishes only a validated structured record. HEIF/AVIF and OCR remain outside this decision.

## Decisions made while entering M3

- Bundled SQLite FTS5 `trigram` is the deterministic Traditional Chinese/English lexical substring baseline (ADR-015).
- Queries shorter than three Unicode characters fail closed; DeskGraph does not substitute an unindexed full-corpus scan.
- External-content indexes follow `locations` and `content_chunks`, while present/active source-of-truth joins determine current visibility.
- Search queries, paths, and snippets may appear only in an explicit user-requested result payload, never ordinary logs or extraction/status payloads.
- Lexical filters are a closed no-model contract: explicit authorized scope, metadata path vs active extracted text, one normalized ASCII-alphanumeric extension, inclusive `modified_since` and exclusive `modified_before` UTC Unix seconds. Applied normalized values are returned to the caller; project/folder filters wait for graph-backed identities.
- D-007 and D-009 remain open: no vector extension, embedding runtime, or model is selected by the lexical slice.

## Decisions made while entering M6

- Filesystem events are untrusted hints. Durable per-scope debounce and a bounded stability gate must complete before the existing atomic manifest scanner reconciles live state (ADR-016).
- The first core uses a one-second default stability window, closed temporary-download suffixes, size/modified-time/platform-identity snapshots, and read-only open-handle identity verification.
- Exact `notify 8.2.0` is selected for target-native hints only. One bounded non-blocking callback queue feeds the existing durable core; watch-set changes, overflow, rescan, source failure and unmatched routing request whole-scope reconciliation, while a second distinct path in one scope requests that scope's durable recovery instead of dropping an ordered rename destination. The five-minute periodic fallback remains enabled. Continuous coalescing is capped at that interval through a root-only forced metadata transaction that rechecks exact authorization/completed-scan eligibility; recovery received during a multi-batch scan forces a fresh follow-up scan. Direct ignored observations cannot cancel unrelated stabilizing work. No content extraction or filesystem action is authorized.
- Watch status is path-free outside explicit adapter/user input. Incremental extraction/indexing, cloud-placeholder policy, background resource controls, and Smart Inbox remain unselected or unimplemented.

## Decisions made while entering M5

- An organization preview is an immutable core-owned record, not mutable frontend or LLM output. The first slice supports only a same-folder rename preview for a present scanned file (ADR-017).
- A preview requires canonical explicit scope containment, a strong manifest identity, matching size/modified time, a matching read-only open handle, a portable single-component target name, and a conflict-free destination.
- Plan plus sequence-1 `preview_created` event commit atomically; both tables reject update/delete. Explicit preview/status may return before/after paths, while logs and recent-plan summaries remain path-free.
- ADR-025 accepts the immutable execution binding, closed append-only command/journal protocol, request receipts and recovery observations as internal foundation. It requires—but does not implement or accept—an OS-lifetime process fence. Its deterministic Unix prototype is test-only and is not a production adapter.
- ADR-026 resolves D-018 by rejecting general macOS/Linux Rename/Move execution for arbitrary user-authorized scopes. Official APIs remain source-pathname based, and a deterministic final-check counterexample moves a replacement inode. Unix Preview/Status/History remain; no feature/configuration/build switch may enable the test adapter.
- ADR-027 resolves D-019's process topology and packaged-identity-first architecture. It does not accept runtime evidence: Windows package family/private-namespace identity must exist before platform fence code; macOS additionally needs a selected OS floor and signed App Sandbox/SIP container proof that a non-entitled same-user probe cannot silently replace the fence entry. If that proof fails, the macOS fence remains unavailable. Every production action entry remains fail closed meanwhile.
- Smart Cleanup may display current evidence before D-017, but no component may expose a system-trash execute control until an ADR fixes platform receipt/restore semantics and the M5 transaction engine passes its fault matrix. Confirmation must bind an immutable candidate/keeper/evidence snapshot; any refresh or source change invalidates the plan instead of silently replacing what the user approved.

## Decisions made while entering M9a

- Packaged Desktop folder authorization originates in the Rust-owned native picker. The WebView has no arbitrary-path authorization command or frontend filesystem/dialog permission; cancellation returns no scope and performs no scan.
- A scope becomes usable in the packaged Desktop only when its database row and opaque platform grant commit atomically and a live runtime capability is held. Legacy rows, malformed or unrestorable grants, canonical-root mismatches and explicit revocation become `needs_reauthorization` rather than silently falling back to a stored path.
- macOS uses a versioned security-scoped-bookmark envelope, balanced start/stop access and stale-bookmark refresh. The grant BLOB never appears in ordinary IPC or logs. The sandbox profile includes `network.client` because a current-host ad-hoc A/B test reproduced the WebKit NetworkProcess permission exit without it and restored the bundled local UI by adding only that key; `network.server` remains absent, production CSP excludes dev-server origins, and core use remains network-independent with no upload client. The checked-in entitlements are configuration evidence only; Developer ID signing, notarization, selected bundle identifier/OS floor, clean-machine restart/stale/revoked/no-egress behavior, Intel-or-Universal and hostile protected-container probes remain release gates.
- Debug-only development receipts keep local development usable when the host cannot issue a bookmark, but every release build rejects them. Windows and Linux release builds fail closed until a real OS/package capability adapter is implemented and accepted; a fixed receipt or successful native dialog selection is not persistent authorization.
- D-003 remains open. The current preferred evidence path is a full MSIX identity shared by Windows OCR and the future action fence, but package name, publisher/certificate identity, distribution channel, update URI and repair/uninstall contract require an explicit owner decision and real Windows acceptance.

## Decisions made while entering M4

- Folder Profiles are read-only, on-demand derivations from current `present` manifest locations and are bounded to 100,000 descendants; overflow fails without a partial result (ADR-018).
- Deterministic direct-child markers may create an explainable Project Suggestion only. README is supporting provenance, not a sufficient strong marker; no heuristic creates `belongs_to` or automatic membership.
- Every suggestion carries basis-point confidence, provenance, completed-scan observation time, `system_rule` creator, fixed provider/version and `model_version: null`.
- Explicit Folder Profile responses may return the selected folder path; structured logs omit paths and member names.
- D-012 is resolved by ADR-019 for durable Project root candidates and exact-root correction. No embedding, vector runtime, model, API, or external dependency is selected by this slice.
- Project root identities, deterministic suggestion observations/signals, and user feedback events are immutable/append-only. Same-decision retries are idempotent; an opposite decision appends a correction.
- A rejected root remains rejected on later proposals with the same stable identity, and an accepted root remains accepted. This is exact-root feedback only and creates no file membership edge.
- ADR-020 adds only explicit same-scope exact-duplicate suggestions for non-empty files up to 64 MiB. Full byte equality, fixed provenance, immutable observations and current source revalidation are required; it adds no merge, deletion, hashing dependency, or file action.
- ADR-021 adds exact-pair feedback only: every explicit accept/reject repeats ADR-020 live verification, events are append-only, retries are idempotent, opposite decisions append corrections, and path-free history never claims current verification. It does not authorize file operations or influence other pairs.
- ADR-022 adds only explicit numeric filename-version suggestions. Same normalized base/extension plus allowlisted `vN` suffixes determine direction; timestamps, sizes, contents, `final`, and models do not.
- ADR-023 binds append-only version feedback to equivalent ordered-node/base/extension/version/provider evidence. Every decision repeats live verification; changed direction returns to `suggested`, and acceptance never authorizes a file action.
- D-013 remains open for cross-root learning. v0.1 may retain append-only user labels and versioned deterministic feature provenance, but a fixed rule score is not a calibrated probability and no online learner may influence another root without a representative evaluation corpus, poisoning/reset/rollback semantics, and an explicit decision.
- D-016 separately keeps deterministic Project merge/split out of scope until stable identity, membership provenance, reversible events and retrieval invalidation are defined; it does not depend on adopting learned scoring.
- ADR-032 accepts only explicit-refresh, manifest-only Project Discovery for one active completed-scan scope. Ordinary results are path-free and capped at 100 deterministic roots with an honest partial flag; one current root path appears only in transient review, and accept/reject repeats current evidence without creating membership or file actions. Background discovery, pagination, membership and learned scoring remain separate decisions.
