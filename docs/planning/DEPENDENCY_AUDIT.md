# Dependency Audit

Last reviewed: 2026-07-19

## Policy

Every new runtime, build, model, binary, crate, npm package, and GitHub Action must record official source, resolved version, purpose, maintenance evidence, supported platforms, license, and security notes. Lockfiles and checksums are authoritative for actual resolved artifacts. Candidates are not approved merely because they appear in this file.

## M0 selected dependencies

| Dependency | Scope | Resolved version | Official source / API evidence | Maintenance and platform evidence | License | Security and decision |
| --- | --- | --- | --- | --- | --- | --- |
| Rust toolchain | Build | 1.97.0 with rustfmt/clippy | `rust-lang.org/tools/install`, official rustup static distribution | Current stable toolchain installed; local macOS arm64 checks pass; remote macOS/Windows/Linux pending | MIT OR Apache-2.0 | `rustup-init` SHA-256 verified before execution |
| Node.js | Build | 24.12.0 primary; 24.1.0 clean-clone check | `nodejs.org` official distribution and release metadata | Supported range is `^20.19.0 || ^22.13.0 || >=24.0.0`; CI pins 24.12.0 on all three OS runners | MIT | Build host only; no Node runtime shipped in the Tauri app |
| Tauri Rust (`tauri`, `tauri-build`) | Runtime/build | 2.11.5 / 2.6.3 | `v2.tauri.app`, crates.io, `tauri-apps/tauri` | Local Rust build, debug bundle, launch, and IPC smoke pass; cross-platform CI pending | MIT OR Apache-2.0 | Capability grants no filesystem/shell plugin; RustSec Linux-path warnings tracked as R-016 |
| Tauri JS API / CLI | Runtime/build | 2.11.1 / 2.11.4 | `v2.tauri.app`, npm, `tauri-apps/tauri` | Local install/build/bundle pass; cross-platform CI pending | MIT OR Apache-2.0 | No filesystem, shell, HTTP, opener, or updater plugin is installed |
| serde / serde_json | Runtime | 1.0.228 / 1.0.150 | crates.io, `serde.rs`, `serde-rs` repositories | Local compile/tests pass; Rust-supported targets claimed upstream | MIT OR Apache-2.0 | Closed health serialization schema; no M0 untrusted deserialization |
| tracing / tracing-subscriber | Runtime | 0.1.44 / 0.3.23 | crates.io, `tokio-rs/tracing` | Local compile/tests pass; active Tokio project | MIT | Fixed event fields, JSON stderr, no target/file/line/span, and explicit path redaction assertions |
| React / React DOM | Runtime UI | 19.2.7 / 19.2.7 | `react.dev`, npm, `facebook/react` | Local typecheck/tests/build and Tauri webview smoke pass | MIT | Presentation only; no direct filesystem capability or raw model output |
| Vite / React plugin | Build | 8.1.4 / 6.0.3 | `vite.dev`, npm, `vitejs` repositories | Local production build passes on Node 24.12.0 | MIT | Development/build only; frozen resolution in `pnpm-lock.yaml` |
| TypeScript | Build | 6.0.3 | `typescriptlang.org`, npm, `microsoft/TypeScript` | Strict typecheck passes | Apache-2.0 | Pinned exactly because TypeScript 7.0.2 exceeded typescript-eslint's declared peer range |
| ESLint / typescript-eslint | Build | 10.7.0 / 8.64.0 | `eslint.org`, `typescript-eslint.io`, npm | `pnpm peers check` and zero-warning lint pass | MIT | Development only; strict peers are enabled and cannot be silently auto-installed |
| Vitest | Test | 4.1.10 | `vitest.dev`, npm, `vitest-dev/vitest` | Nineteen frontend contract tests pass locally | MIT | Node environment only; no browser emulator dependency |
| pnpm | Build | 11.10.0 | `pnpm.io`, npm, `pnpm/pnpm` | Corepack activation, lockfile, frozen-compatible install, and peer check pass | MIT | Exact `packageManager`; strict peers and supply-chain policy check enabled |
| Prettier | Build | 3.9.5 | `prettier.io`, npm, `prettier/prettier` | Repository formatting check passes | MIT | Development only; planning/prompts are excluded to preserve the supplied SSOT text |
| cargo-audit | Audit tool, not shipped | 0.22.2 | crates.io, `docs.rs/cargo-audit`, `rustsec/rustsec` | RustSec official project; requires Rust 1.88+; local audit executed | Apache-2.0 OR MIT | Installed outside the project and not added to the application dependency graph |

## M1 selected dependencies

| Dependency | Scope | Selected version | Official source / API evidence | Maintenance and platform evidence | License | Security and decision |
| --- | --- | --- | --- | --- | --- | --- |
| `rusqlite` / `libsqlite3-sys` | Runtime | 0.40.1 / 0.38.1 | crates.io, `docs.rs/rusqlite`, `rusqlite/rusqlite`; official examples confirm `Connection`, transactions, batch pragmas, bundled SQLite, and progress-handler interruption | Current crates.io release inspected on 2026-07-16; upstream supports Rust desktop targets through SQLite and `cc` | MIT | Disable rusqlite defaults and enable only `bundled` plus `hooks`; this avoids an unknown system SQLite/default WASM backend and supplies the bounded read-only MCP query deadline. Manifest migrations are embedded and checksummed. The hook adds no package or callback capability outside the local connection. |
| `unicode-normalization` | Runtime | 0.1.25 | crates.io, `docs.rs/unicode-normalization`, `unicode-rs/unicode-normalization` | Current crates.io release inspected on 2026-07-16; pure Rust and platform-independent | MIT OR Apache-2.0 | NFC comparison keys reduce canonically equivalent path duplicates. This is not a security substitute for canonical scope validation. |
| `windows-sys` | Windows runtime only | 0.61.2 | crates.io, Microsoft `microsoft/windows-rs`, generated Windows API docs; verified signatures for `CreateFileW`, `GetFileInformationByHandle`, `CloseHandle`, `BY_HANDLE_FILE_INFORMATION`, and package-identity-only `GetCurrentPackageFullName` | Current crates.io release inspected on 2026-07-16; Microsoft-maintained; Rust 1.71 minimum; Windows-only target dependency | MIT OR Apache-2.0 | Workspace features are `Win32_Foundation`, `Win32_Security`, `Win32_Storage_FileSystem`, and `Win32_Storage_Packaging_Appx`. Unsafe calls stay in the identity or OCR preflight adapters. OCR requests only the package-name buffer length and never reads/logs the package name. ADR-027 identifies future private-namespace, mutex, DACL, wait, handle-inheritance, and package-family APIs, but no feature or production call is accepted until the Windows package identity and real runtime matrix exist. |
| `clap` | CLI runtime | 4.6.2 | crates.io, `docs.rs/clap`, `clap-rs/clap`; official derive and nested subcommand examples inspected | Current crates.io release inspected on 2026-07-16; Rust 1.85 minimum; pure Rust CLI parser | MIT OR Apache-2.0 | Schema-derived CLI rejects ambiguous input and supports future M1 job controls without custom parsing. |
| `tempfile` | Tests/bench fixtures only | 3.27.0 | crates.io, `docs.rs/tempfile`, `Stebalien/tempfile` | Current crates.io release inspected on 2026-07-16; Rust 1.63 minimum; cross-platform | MIT OR Apache-2.0 | Dev-only fixture isolation. Never used by product runtime or as a permanent-delete product capability. |

### M1 dependency verification notes

- Rust 1.97 `std::os::windows::fs::MetadataExt::{volume_serial_number,file_index,number_of_links}` was compiled against `x86_64-pc-windows-msvc` and rejected with `E0658 windows_by_handle`; it is not a viable stable implementation.
- The selected Microsoft binding exposes the required stable Win32 APIs. Windows CI must compile and run identity fixtures before M1 can be considered cross-platform verified.
- `rusqlite` currently defaults to `cache` and `ffi-sqlite-wasm-rs`; DeskGraph explicitly opts out of defaults and selects bundled native SQLite.
- The M1 lockfile resolves 456 crate dependencies. `cargo audit --no-fetch` against 1,160 cached RustSec advisories found zero known vulnerabilities and the same 17 Tauri Linux-path warnings tracked in R-016; the new M1 direct dependencies added no advisory.

## M2 extraction dependency decisions

The first M2 provider adds **no external dependency**. Plain text, Markdown, and source code use Rust standard-library `Read + Seek`, UTF-8 validation, bounded buffering, chunking, and time/cancellation checks. Durable jobs and content chunks reuse the already audited `rusqlite` database layer; open-file identity reuses the existing `unicode-normalization` and `windows-sys` boundary; `tempfile` remains test-only. The `Cargo.lock` changes for this slice only connect existing DeskGraph workspace crates and do not introduce a new registry package.

This initial decision keeps the core usable without Python, Docker, Ollama, a model, an API key, or network access. It did not preapprove later ZIP/XML, image, OCR, model, or native runtime candidates; each accepted later delta is recorded separately below.

### PDF text dependency selected

| Dependency | Scope | Selected version | Official source / API evidence | Maintenance and platform evidence | License | Security and decision |
| --- | --- | --- | --- | --- | --- | --- |
| `lopdf` | PDF runtime | `0.44.0`, exact, `default-features = false` | crates.io, `docs.rs/lopdf`, `J-F-Liu/lopdf`; verified `LoadOptions::max_decompressed_size`, `Document::load_mem_with_options`, `extract_text_with_limit`, `get_pages`, `is_encrypted`, and `was_encrypted` in the published source | Released 2026-07-10; upstream active when inspected 2026-07-16; Rust 1.88 minimum; minimal crate test passed on macOS arm64 and Windows x64 cross-check compiled; no native PDF library | MIT | Accepted by ADR-013 for strict, bounded, path-free text-layer extraction only. Default features are forbidden. Actions, JavaScript, attachments, annotations, multimedia, external references, passwords, write APIs, and unbounded extraction APIs are outside the adapter. |

The isolated no-default-feature graph resolves 53 registry packages. All report license expressions and all provide a permissive licensing path; final notices/SBOM remain an M9 gate. `cargo audit --no-fetch` with 1,160 cached advisories reported zero vulnerabilities and zero warnings for this minimal graph. By contrast, lopdf's upstream full-feature lock contains vulnerable `crossbeam-epoch 0.9.18` (`RUSTSEC-2026-0204`); that graph is rejected, and CI must keep proving that `crossbeam-*`/Rayon do not enter DeskGraph through this dependency.

The load API limits each eagerly decompressed object or cross-reference stream, and the extraction API limits each page and `/ToUnicode` stream. It does not expose a whole-document aggregate allocator budget. DeskGraph therefore also enforces source bytes, page count, sequential page processing, stored output/chunks, cooperative time/cancellation, and keeps peak residency on an 8 GB machine as an open release gate (R-005/R-007).

`pdf-extract 0.12.0` is rejected: its published `extract_text*`/`extract_text_by_pages*` functions call unbounded `Document::load*` and output traversal, use `lopdf 0.42`, and accept no decompression, page, output, time, or cancellation policy.

### Image metadata dependency selected

| Dependency | Scope | Selected version/features | Official source / API evidence | Maintenance and platform evidence | License | Security and decision |
| --- | --- | --- | --- | --- | --- | --- |
| `imagesize` | Encoded image format and dimensions only | `0.15.0`, exact, `default-features = false`; only `bmp`, `gif`, `jpeg`, `png`, `tiff`, `webp` | crates.io and `Roughsketch/imagesize`; inspected published `reader_type` plus `ImageType::reader_size` reader APIs and the complete selected-format source | Released 2026-07-09; pure Rust; selected graph has no transitive dependency; isolated test passed on macOS arm64 and `x86_64-pc-windows-msvc` checked with pinned Rust 1.97.0 | MIT | Accepted for bounded header metadata only. DeskGraph never calls the crate's filesystem helper, decodes pixels, enables HEIF/AVIF, or extracts EXIF/GPS/arbitrary strings. |

The adapter receives only a core-controlled in-memory probe cursor. DeskGraph independently caps source/probe bytes, read/seek operations, time/cancellation, each dimension, and total encoded pixels; it verifies extension-to-signature agreement plus stricter format headers before publication. Metadata is stored in a dedicated structured table with source/provider provenance and no path, filename, EXIF, GPS, or fake text chunk. Malformed, mismatched, truncated, over-limit, cancelled, and changed sources publish nothing.

The isolated lock contains only the fixture root and `imagesize`; `cargo tree` confirms the six selected features and no transitive package. The published source scan found no `unsafe`, network, or process execution in the selected parsing path. `cargo audit --no-fetch` against 1,160 cached advisories reports zero findings for the isolated graph. The exact dependency adds one package to DeskGraph's lock and no new full-lock advisory. HEIF/AVIF remains deliberately excluded because its nested box traversal and format-specific size arithmetic have not passed DeskGraph's separate resource/safety contract; support is not implied by upstream capability.

### Screenshot OCR architecture and selected native bindings

ADR-024 selects a native-first architecture, not a blanket approval for every OCR runtime. Apple Vision is the macOS provider and the already locked Microsoft `windows 0.61.3` binding is accepted for a package-identity-gated `Windows.Media.Ocr` provider. D-015 reopens the packaged fallback implementation so Tesseract and PP-OCRv6 can be measured on the same corpus and release gates. `ocrs` remains rejected for v0.1 because its official model support does not cover Traditional Chinese.

| Dependency | Scope | Selected version/features | Official source / API evidence | Maintenance and platform evidence | License | Security and decision |
| --- | --- | --- | --- | --- | --- | --- |
| `objc2-vision` | macOS OCR runtime binding only | `0.3.2`, exact, `default-features = false`; `VNObservation`, `VNRecognizeTextRequest`, `VNRequest`, `VNRequestHandler`, `VNTypes`, `block2`, `objc2-core-foundation`, `std` | Apple [`VNRecognizeTextRequest`](https://developer.apple.com/documentation/vision/vnrecognizetextrequest)/[`VNImageRequestHandler`](https://developer.apple.com/documentation/vision/vnimagerequesthandler); published crate source and [`docs.rs/objc2-vision`](https://docs.rs/objc2-vision/0.3.2/objc2_vision/); verified bytes-only `initWithData:options:`, language capability, accurate recognition, top candidates, confidence, normalized bounding boxes and synchronous perform APIs; progress-handler-to-`cancel` binding compiles but runtime cancellation remains an implementation gate | [`madsmtm/objc2`](https://github.com/madsmtm/objc2) is the upstream repository; published metadata supports macOS arm64/x64 and Rust 1.71+. Rust 1.97 isolated compile and local macOS arm64 language/recognition probes pass; Intel and clean remote runtime remain | Zlib OR Apache-2.0 OR MIT | Accepted only as a target-specific thin binding. No model, network, subprocess, path/URL input, dynamic discovery, or user-installed runtime. Runtime buffers and output remain DeskGraph-bounded; OS model cache unload cannot be claimed. |
| `windows` | Windows package-identity-gated OCR binding only | `0.61.3`, exact; this target edge sets `default-features = false` and requests `std`, `Globalization`, `Graphics_Imaging`, `Media_Ocr`, `Storage_Streams`, `Win32_System_WinRT` | Microsoft [`Windows.Media.Ocr`](https://learn.microsoft.com/en-us/uwp/api/windows.media.ocr), [`OcrEngine`](https://microsoft.github.io/windows-docs-rs/doc/windows/Media/Ocr/struct.OcrEngine.html), [`OcrWord`](https://microsoft.github.io/windows-docs-rs/doc/windows/Media/Ocr/struct.OcrWord.html), and published source; verified in-memory bytes, decode, actual recognizer language, `TextAngle`, word boxes, MTA initialization, async status/cancel/terminal-close bindings | [`microsoft/windows-rs`](https://github.com/microsoft/windows-rs) is Microsoft-maintained; MSRV 1.74; exact Rust 1.97 isolated API probe and production Windows cfg check/Clippy pass. Runtime still requires real Windows, package identity, and installed recognizers | MIT OR Apache-2.0 | Accepted as a target-specific thin binding already present through Tauri, so it adds no lock package. Cargo features are additive: this direct-edge list is not the globally resolved feature set; release evidence uses `cargo tree --target x86_64-pc-windows-msvc -e features -i windows@0.61.3`. No package identity returns unavailable; incompatible language resolution returns language unavailable; no automatic fallback exists yet. |
| `windows-future` | Windows OCR async operation types only | `0.2.1`, exact, `default-features = false` | Published Microsoft source exposes `IAsyncOperation<T>` and `AsyncStatus`; production uses only `Status`, `Cancel`, `Close`, and `GetResults` | Microsoft-maintained `microsoft/windows-rs`; MSRV 1.74; exact isolated and production Windows cfg checks pass | MIT OR Apache-2.0 | Required as a direct dependency because `windows` does not re-export the generic async types used by the bounded lifecycle adapter. The same version was already locked; no network, process, path, or runtime discovery capability is introduced. |

The exact published crate archive is 69,317 bytes with SHA-256 `bfc194758a2d5d7540b1ad283bfb9ca318ec608991892326e95b428230b2689b`. The complete isolated lock has nine packages: the fixture plus `objc2-vision 0.3.2`, `block2 0.6.2`, `dispatch2 0.3.1`, `objc2 0.6.4`, `objc2-core-foundation 0.3.2`, `objc2-encode 4.1.0`, `objc2-foundation 0.3.2`, and `bitflags 2.13.1`. All except `objc2-vision` were already present in DeskGraph's lock, so the target-specific workspace adoption grew the lock from 492 to 493 packages. `cargo audit --no-fetch` scanned both the isolated nine-package lock and that 493-package workspace lock against 1,160 cached advisories: the OCR delta had no finding, while the workspace retained the same 16 unmaintained plus one `glib` unsound warnings from existing Tauri/Linux paths.

The implemented provider receives only core-validated PNG/JPEG bytes plus dimensions, limits, deadline and cancellation state. It independently caps input at 32 MiB, dimensions at 16,384, total pixels at 64 Mi pixels, output at 8 MiB, observations at 4,096 and each observation at 256 KiB; all spatial/confidence output is revalidated before atomic SQLite/FTS publication. The local runtime reported both `en-US` and `zh-Hant`. A current-binary controlled mixed-language PNG recognized `DeskGraph OCR` and `桌面圖譜 安全整理` with valid normalized top-left bounding boxes when `zh-Hant` was ordered before `en-US`; the completed job used 51,661 source bytes, 38 output bytes, two chunks and 1,153 ms outside the restricted sandbox. The restricted runner returned a fixed provider failure with zero published output while its language probe still succeeded. This is local arm64 implementation evidence only, not macOS Intel, clean-machine, memory, representative corpus, actual native cancellation, release, or M2 completion evidence.

The published `windows 0.61.3` crate is 9,372,520 bytes with lock/archive SHA-256 `9babd3a767a4c1aef6900409f85f5d53ce2544ccdfaa86dad48c91782c6d6893`; `windows-future 0.2.1` is 17,532 bytes and `windows-sys 0.61.2` is 2,517,186 bytes. All three are Microsoft `windows-rs`, MIT OR Apache-2.0, and already present in the lock through Tauri or the identity adapter. The OCR direct edges therefore kept that slice at 493 packages and introduce no second 0.61 binding version. The following evaluator slice reached 494 and the later native-Watch slice reaches 511; both full-lock audits report zero vulnerabilities. A 0.62 binding family is also already present elsewhere in the workspace lock, but the extractor deliberately reuses the Tauri-compatible 0.61 family instead of adding another generated-binding family to this crate without an API benefit.

Microsoft documents that `Windows.Media.Ocr` is supported for desktop apps only with package identity and that requested languages resolve against available OCR recognizers. The implementation requests `zh-TW` and `en-US`, then validates the actual `RecognizerLanguage` as Traditional Chinese (never `zh-Hans`) or English before merging bounded output; it never invokes DISM, Settings, the network, or silent installation. `OcrWord` exposes text and a rectangle but no confidence, so migration 0016 keeps the box mandatory while storing confidence as `NULL`. Only absent/zero `TextAngle` may publish source-aligned boxes; rotated/non-finite results fail closed. The owned single-worker lifecycle bounds caller return, waits for terminal status before `Close()`, and prevents stuck cleanup from accumulating. Host policy/state-machine tests and Windows cfg check/Clippy pass; real Windows 10/11 x64, MSIX/external-location identity, present/missing language resolution, native cancellation/cleanup recovery, mixed corpus, RSS/8 GB, and installer/fallback evidence remain runtime/release gates.

### M2 OCR dependencies still requiring selection and audit

| Capability | Current status | Required evidence before adoption |
| --- | --- | --- |
| Windows native OCR binding/provider | Exact existing binding accepted; provider code plus host-policy and Windows-cfg gates pass; runtime open | Package identity present/absent, requested/resolved language behavior, real Windows cancellation/cleanup/single-worker recovery, mixed-language/no-text/corrupt/limit runtime, RSS/8 GB, MSIX and fallback routing evidence |
| Tesseract fallback candidate | Models verified; wrapper/runtime rejected or unaccepted | Official [`tessdata_fast`](https://github.com/tesseract-ocr/tessdata_fast) 4.1.0 commit `65727574dfcd264acbb0c3e07860e4e9e9b22185`; `eng.traineddata` 4,113,088 bytes SHA-256 `7d4322bd2a7749724879683fc3912cb542f19906c83bcc1a52132556427170b2`; `chi_tra.traineddata` 2,366,642 bytes SHA-256 `529c5b5797d64b126065cd55f2bb4c7fd7b15790798091b1ff259941a829330b`. `tesseract-ocr-static-c 0.1.2` has monitor lifetime/iteration/panic and artifact/platform concerns; `tesseract-rs 0.1.20` has unverified downloads/cache, missing `chi_tra`, no monitor cancellation and unsafe `Send`/`Sync`; `tesseract-static 0.2.1` permits unverified dynamic libraries. No binding is accepted. A narrow owned adapter still needs pinned Tesseract/Leptonica artifacts, blocking macOS arm64/Intel and Windows x64 builds, nonblocking Linux experimental evidence, bytes-only init, cancellation/deadline, notices/SBOM and RSS/unload evidence. |
| PP-OCRv6 fallback candidate | Official model evidence found; runtime unaccepted | Official [PP-OCRv6 documentation](https://www.paddleocr.ai/latest/en/version3.x/algorithm/PP-OCRv6/PP-OCRv6.html) supports Traditional Chinese/English. Official `small_det` [ONNX artifact](https://huggingface.co/PaddlePaddle/PP-OCRv6_small_det_onnx/tree/main) is about 9.88 MB and `small_rec` [ONNX artifact](https://huggingface.co/PaddlePaddle/PP-OCRv6_small_rec_onnx/tree/main) about 21.2 MB under Apache-2.0; upstream Apple M4/accuracy results are not DeskGraph evidence. The official [Windows C++ deployment](https://www.paddleocr.ai/latest/en/version3.x/inference_deployment/local_inference/cpp/OCR_windows.html) also requires a larger native closure than model files alone imply. Require exact artifact hashes, preprocessing/postprocessing contract, ONNX/runtime dependency closure, no telemetry/network, bytes-only API, cancellation/deadline, arm64/x64/Windows/Linux packaging, notices/SBOM, RSS/unload and the same evaluation corpus before adoption. |

No fallback router is implemented while D-015 is open. Adding a dormant router would expose no capability and would prematurely freeze provider ownership and lifecycle contracts. When a fallback is selected, routing must be delivered with its provider and end-to-end tests. Eligibility requires a separate successful capability preflight proving unsupported platform, missing package identity or missing/incompatible language; the current `OcrProviderUnavailable` code alone is too broad because it also covers worker-busy, thread/COM/stream/API and other lifecycle failures. Cancellation, deadline, busy/resource pressure, runtime/provider failure, invalid output, source change, resource limit and policy failures remain terminal and may not be hidden by retrying a second provider. Provider confidence values are not assumed calibrated across engines.

### OCR evaluator reproducibility dependency

The D-015 OCR evaluator and macOS Vision runner are workspace-only evidence tools, not product runtime code and not a fallback provider, router, model downloader, signature verifier, or installer component. Exact `sha2 =0.11.0` with `default-features = false` calculates SHA-256 over exact corpus, asset-manifest, image, and run bytes. Those digests bind reported evidence reproducibly; they are not authenticity, provenance, signing, or trust verification.

| Dependency | Scope | Selected version/features | Official source / API evidence | Maintenance and platform evidence | License | Security and decision |
| --- | --- | --- | --- | --- | --- | --- |
| `sha2` | Workspace OCR evaluator/runner and M5 action binding | `0.11.0`, exact, `default-features = false` | RustCrypto [`hashes`](https://github.com/RustCrypto/hashes); published `Sha256` / `Digest` API | Pure Rust, MSRV 1.85, cross-platform; `sha2 0.11.0` was already locked transitively through `lopdf` | MIT OR Apache-2.0 | Accepted for bounded SHA-256 evidence binding. OCR tooling hashes bounded in-memory evidence; M5 streams at most 8 GiB from an identity-bound source handle through a 64 KiB buffer with a 90-second deadline. Digests detect changed bytes but do not provide authenticity, authorize an action, or close the Unix source-leaf syscall race. The 33,969-byte published crate archive has SHA-256 `446ba717509524cb3f22f17ecc096f10f4822d76ab5c0b9822c5f9c284e825f4`. No network, process, model, or signing claim is introduced. |
| `libc` | Unix Watch delta, action identity, test-only Rename counterexample/protocol adapter, and macOS OCR evidence runner | `0.2.186`, exact, `default-features = false` | Official Rust Project [`rust-lang/libc`](https://github.com/rust-lang/libc); published Unix/Apple bindings expose `open`/`openat`, `fstatat`, `fsync`, `O_NOFOLLOW`, `O_DIRECTORY`, `O_CLOEXEC`, `O_NONBLOCK`, `O_NOCTTY`, Apple `O_NOFOLLOW_ANY`, `renameatx_np` and `RENAME_EXCL` | Rust Project maintained raw FFI crate; MSRV 1.65; cross-platform crate. Watch/action edges are Unix-gated; the evidence-runner publication edge remains macOS-gated | MIT OR Apache-2.0 | Accepted narrowly for descriptor-bound Watch/action identity checks, the existing runner publication boundary, and test-only rejection evidence. ADR-026 rejects a general production macOS/Linux Rename/Move adapter: the native leaf-name syscalls do not atomically bind the source inode, and a deterministic test proves that a replacement after final revalidation is moved. ADR-027 permits a future macOS `flock` fence only after signed App Sandbox container/scope identity and runtime evidence; it adds no production call or dependency now. The 821,883-byte published archive has lock/archive SHA-256 `68ab91017fe16c622486840e4c83c9a37afeff978bd239b5293d61ece587de66`. Every unsafe call has local ownership/lifetime justification. No path-based production Rename, unlink, deletion, shell or arbitrary filesystem API is accepted. |

The evaluator/runner direct edges added no registry package: `sha2 0.11.0`, macOS-only runner use of `libc 0.2.186`, and dev-only `tempfile 3.27.0` were already locked; `deskgraph-domain`, `deskgraph-extractors`, and `deskgraph-identity` are local workspace crates that let the runner reuse production formats, limits/output validation, image probing, platform identity, and reparse policy instead of copying them. The evaluator requests no `sha2` default features, while a full unified workspace build still activates `sha2`'s `alloc`/`oid` defaults through `lopdf`; the tool itself uses only `Digest`/`Sha256`. Later Unix-gated Watch and M5 edges reuse the exact pinned `libc`; M5 also reuses `sha2` for the bounded source binding. Neither adds a registry package, model, API, network, process runner, shell, or general filesystem plugin. `tempfile` is test-only. The current lock remains 518 packages after M7; the M5 dependency-edge change adds no advisory-bearing artifact.

### Office OOXML dependencies selected

ADR-014 accepts exact `zip =8.6.0` (`zip-rs/zip2`, MIT, Rust 1.88+) with `default-features = false` plus only `deflate-flate2-zlib-rs`, and exact `quick-xml =0.41.0` (`tafia/quick-xml`, MIT, Rust 1.79+) with `default-features = false`. The selected graph contains no archive crypto, Bzip2, Deflate64, LZMA, PPMd, XZ, Zstandard, async, Serde, encoding, or native C compression feature.

The archive API exposes central-directory construction, entry count, encrypted status, compressed/uncompressed sizes, overlap detection, enclosed-name validation, compression method, and bounded `Read`. The XML API exposes streaming `NsReader`, bounded namespace declarations, and explicit start/end/text/CDATA/DTD/processing-instruction/general-reference events. DeskGraph resolves only the transitional/strict WordprocessingML, DrawingML, and SpreadsheetML namespaces needed to prevent cross-namespace tag confusion; it does not use recursive extraction, entry writes, relationship traversal, password APIs, entity expansion, or whole-document deserialization.

The isolated exact lock contains 14 registry packages: `adler2 2.0.1`, `cfg-if 1.0.4`, `crc32fast 1.5.0`, `equivalent 1.0.2`, `flate2 1.1.9`, `hashbrown 0.17.1`, `indexmap 2.14.0`, `memchr 2.8.3`, `miniz_oxide 0.8.9`, `quick-xml 0.41.0`, `simd-adler32 0.3.10`, `typed-path 0.12.3`, `zip 8.6.0`, and `zlib-rs 0.6.6`; only the selected feature tree is active at build time. Their expressions are MIT, Apache-2.0, BSD-like, Unlicense, or Zlib compatible; notices/SBOM remain an M9 gate. With 1,160 cached advisories, `cargo audit --no-fetch --json` reports zero vulnerabilities and zero warnings. `quick-xml 0.41.0` is the patched floor for namespace-resolver issue `RUSTSEC-2026-0195`; the provider intentionally uses that patched resolver with a 128-declaration per-element cap. The minimal graph tests on macOS arm64 and checks for Windows x64 using Rust 1.97.0.

The selected adapter never writes archive entries to disk or follows relationships. It selects only exact DOCX/PPTX/XLSX text parts, rejects encryption/overlap/unsafe or duplicate selected names/unsupported compression, bounds claimed and actual decompression plus structure/output/time, rejects DTD and unsupported entities, keeps macros/formulas/external links/embeddings inert, and requires explicit paragraph/slide/cell provenance.

The exact two dependencies entered the Rust workspace only with the accepted feature set. No high-level Office parser, additional archive/XML feature, native binary, model, package-owned build script, or relationship-following behavior is approved. Provider adversarial fixtures and the complete-lock RustSec delta now pass locally; remote runtimes, representative real-world corpora, redistribution notices/SBOM, and 8 GB measurement remain required before M2 completion.

## M3 lexical-search dependency decision

The first M3 slice adds no external package. ADR-015 reuses the already selected bundled SQLite from `rusqlite 0.40.1` / `libsqlite3-sys 0.38.1` and its built-in FTS5 `trigram` tokenizer. SQLite's official FTS5 documentation confirms external-content indexes, synchronization triggers and rebuilds, trigram substring behavior, the three-Unicode-character minimum, `rank`/BM25 ordering, and bounded `snippet()` output. Local migration and multilingual tests prove the selected bundled build exposes FTS5.

No vector extension, tokenizer extension, embedding runtime, model, API, or network client is selected by this decision. The workspace adds only path-based local `deskgraph-retrieval` and `deskgraph-search-benchmark` crates; they introduce no registry dependency and keep future vector adapters outside the database and domain contracts. The benchmark tool reuses audited `clap`, `rusqlite`, `serde`, and `serde_json`, refuses to overwrite an existing database, and is not shipped with the product.

## M6 durable watch-core dependency decision

The first M6 slice adds only the local path-based `deskgraph-watcher` workspace crate. It reuses the audited database/domain/identity/scanner layers and Rust standard library; no native watcher, async runtime, network client, or registry package was added. Native adapter candidates remain unapproved until official API, maintenance, platform, license, and security evidence exists.

The second M6 slice also adds no registry package. The Tauri coordinator, bounded schedule, metadata-scan gate, stop flag, worker thread and metadata-only polling fallback use the Rust standard library plus already-locked workspace/Tauri/Serde dependencies. `serde` became an explicit Desktop Rust dependency only for the closed path-free runtime response; it was already present in `Cargo.lock`. Forward migration 0017 adds only a partial SQLite index for active watch status/deadline ordering. The fallback never compares file contents, never watches an unscanned authorization, and routes every scheduled scope root through the existing canonical scope, durable stability and atomic scanner boundaries.

### Native event adapter — adopted with a periodic safety net

| Dependency | Scope | Selected version/features | Official source / API evidence | Maintenance and platform evidence | License | Security and decision |
| --- | --- | --- | --- | --- | --- | --- |
| `notify` | Target-native filesystem hints | `8.2.0`, exact, `default-features = false`; macOS adds only `macos_fsevent` | Tagged [`notify` manifest](https://github.com/notify-rs/notify/blob/notify-8.2.0/notify/Cargo.toml), [`Watcher` implementation](https://github.com/notify-rs/notify/blob/notify-8.2.0/notify/src/lib.rs), and [`Event`](https://github.com/notify-rs/notify/blob/notify-8.2.0/notify-types/src/event.rs) verify `RecommendedWatcher`, target backends, ordered event paths, `need_rescan`, and callback requirements | Released and inspected 2026-07-18; upstream selects FSEvents on macOS, `ReadDirectoryChangesW` on Windows, inotify on Linux and kqueue on BSD/iOS. Rust 1.77 minimum; exact macOS arm64 live source plus Desktop create/modify/rename/delete tests pass outside the restricted test sandbox. Target dependency trees resolve on macOS arm64, Windows x64, Linux x64 and FreeBSD x64; native runtime evidence outside macOS arm64 remains open | CC0-1.0 | Accepted only as an untrusted event-hint adapter. It has no database, graph, content, model, network, process, file-action or durable debounce authority. No `notify-debouncer-*` package is used. |

The shared adapter owns one native watcher and prefix-minimizes physical roots while retaining every logical scope. Its callback performs no filesystem reads, canonicalization, database work, scan, extraction, logging or blocking send. It accepts at most two absolute paths and 16 KiB of path bytes per upstream event, writes into a 256-item `try_send` queue, and emits a capacity-one wake token. Empty/relative/oversized events, queue overflow, `need_rescan`, source error and unmatched routing all fail over to path-free whole-scope durable reconciliation. At most 64 native signals drain per coordinator cycle and downstream work retains at most one ordinary hint per logical scope; a second distinct path requests durable recovery for that scope, preserving ordered rename old/new semantics without unbounded hint expansion. Every accepted hint still passes ADR-016 scope, symlink/reparse, temporary-download, stability, read-only open-handle identity and atomic scanner gates. The existing five-minute reconciliation remains enabled for missed events and unsupported/native-failure cases.

Migration 0018 adds a durable monotonic reconciliation kind. An ordinary Unix hint for one already-manifested, single-link regular file may now reuse the exact pinned `libc 0.2.186` package to open every root component, the parent and the leaf descriptor-relatively with no-follow flags, compare two complete anchored snapshots, and hold the second handle set through one atomic metadata publication CAS. This adds no registry package and deliberately does not adopt `notify-debouncer-*` or a second scanner/finalizer. Any create, missing path, directory, hard link, identity/topology ambiguity, active/interrupted scan, non-Unix target or CAS conflict promotes the durable event to full scope; it cannot demote later. The delta invalidates only the affected node's stale extraction output and cannot extract content, update graph edges, authorize actions or mutate files. `Cargo.lock` therefore remains at 511 packages.

The lock grew from 494 to 511 packages. The 17-package delta is `notify 8.2.0`, `notify-types 2.1.0`, `fsevent-sys 4.1.0`, `inotify 0.11.4`, `inotify-sys 0.1.8`, `kqueue 1.2.0`, `kqueue-sys 1.1.2`, `windows-sys 0.60.2`, `windows-targets 0.53.5`, and eight `windows_* 0.53.1` architecture packages. Exact manifests report CC0-1.0 for `notify`; MIT OR Apache-2.0 for `notify-types` and the Windows binding family; MIT for FSEvents/kqueue bindings; and ISC for inotify bindings. Existing `mio`, `walkdir`, `same-file`, `libc`, `log` and `bitflags` packages are reused. `cargo audit --no-fetch` scanned the 511-package lock with zero vulnerabilities and the same 17 accepted Tauri/Linux warnings. Final redistribution notices and generated SBOM remain M9 gates.

Known limits remain explicit: native events can be incomplete, duplicated or reordered; NFS/WSL/network mounts may emit nothing; macOS FSEvents can have ownership/coalescing limits; Linux inotify and kqueue have resource ceilings. The restricted Codex filesystem sandbox suppresses FSEvents, while the exact same source and Desktop integration tests pass outside it; release CI must therefore run native tests in a normal host context. Windows x64, Linux x64, macOS Intel/Universal, root replacement, real kernel overflow/event storms, large-tree CPU/RSS/thermal and 8 GB behavior remain required evidence before M6 or release completion.

## M5 rename-protocol dependency decision

The first M5 preview slice added only the local path-based `deskgraph-transactions` workspace crate. The protocol foundation now gives that crate direct edges to already locked and audited `sha2 0.11.0`, Unix-only `libc 0.2.186`, and `serde 1.0.228`; `deskgraph-identity` reuses Unix-only `libc` for descriptor-bound action identity and dev-only `tempfile`. `cargo tree -p deskgraph-transactions --offline -e normal` confirms the intended local database/domain/identity/scanner graph plus those existing packages. `Cargo.lock` remains at 518 packages; no new registry artifact, file-operation plugin, async runtime, model, network client, shell, Python, Docker, Ollama, or native runtime was added.

`sha2` streams the preview's identity-bound source bytes under the fixed 8 GiB/90-second/64 KiB limits. `libc` supplies no-follow descriptor checks; its no-replace macOS/Linux rename calls compile only in tests because ADR-026 resolves D-018 by rejecting general Unix execution. The additional counterexample uses the same locked `libc` edge and adds no package. No process-fence dependency or implementation is accepted. Production adapters return unsupported before database, journal, lock, or source mutation. These reused dependencies therefore support an auditable fail-closed protocol and do not establish a shipped filesystem-action capability.

## M4 folder-profile dependency decision

The M4 Folder Profile slice adds only the local path-based `deskgraph-projects` workspace crate. The later Project root correction, exact-duplicate relation, exact-pair feedback, and filename-version flows add no registry package: they promote the already-audited local scanner/identity layers to runtime use and rely on SQLite plus bounded Rust code rather than a hashing or graph library. ADR-022 adds a direct `deskgraph-domain` use of the already locked/audited pure-Rust `unicode-normalization 0.1.25` package (MIT OR Apache-2.0, platform-independent, official `unicode-rs/unicode-normalization`) so database and filesystem boundaries share one NFC rule; `Cargo.lock` remains 488 packages and changes only the local package dependency list. `serde_json` and `tempfile` remain test-only. No new package, embedding/vector runtime, hash package, model, API, network client, shell, Python, Docker, Ollama, or native runtime was added. ADR-018 through ADR-022 require deterministic current manifest facts, immutable evidence, exact-root/exact-pair correction, bounded byte equality, and explicit filename evidence before future similarity, larger-file hashing, or learned scoring.

## M8 four-locale UI dependency decision

The English, Traditional Chinese, Simplified Chinese, and Japanese Desktop catalogs are typed TypeScript modules bundled by the existing Vite build. One local registry supplies locale tags, autonyms, HTML language/direction metadata and catalog selection; the selector is a native HTML control, and locale-aware numbers/dates use the built-in `Intl` runtime. Conservative BCP 47 recognition and the storage allowlist are repository code with closed tests.

No i18n package, remote translation service, lazy language downloader, model, parser, network client, Tauri plugin/permission, CSP source, migration, Python, Docker or Ollama dependency was added. `pnpm-lock.yaml`, `Cargo.lock` and the packaged application capability set remain unchanged. Future plural rules or catalog extraction tooling must be evaluated from actual product complexity rather than assumed now; installer, updater and release-document localization remains an M9/M10 gate.

## M7 read-only MCP dependency decision

| Dependency | Scope | Selected version/features | Official source / API evidence | Maintenance and platform evidence | License | Security and decision |
| --- | --- | --- | --- | --- | --- | --- |
| `rmcp` | Standalone MCP stdio server only | `2.2.0`, exact, `default-features = false`; `server`, `transport-io` | Official [Model Context Protocol Rust SDK](https://github.com/modelcontextprotocol/rust-sdk), published crate metadata/source, and the stable [MCP 2025-11-25 lifecycle and transports specification](https://modelcontextprotocol.io/specification/2025-11-25/basic/transports) were inspected on 2026-07-18. Production uses the SDK lifecycle, tool models and Tokio stdio transport rather than a handwritten protocol state machine | Released 2026-07-08 and maintained in the official protocol organization. The SDK is portable Rust/Tokio stdio, while DeskGraph enables this reader only on macOS/Linux because the local SQLite sidecar contract is platform-specific; macOS arm64 compile and real child-process tests pass, Linux/macOS Intel runtime remain, and Windows fails closed. The crate declares edition 2024 but no explicit MSRV; DeskGraph's pinned Rust 1.97 passes | Apache-2.0 | Accepted only for one local read-only server with minimal features. Defaults, client, auth, HTTP/SSE/streamable HTTP, macros, schema generation, elicitation, sampling and model capabilities are excluded. The SDK's line reader is not independently size-bounded, so DeskGraph places a tested 64 KiB draining frame gate before it and filters non-DeskGraph protocol logging. |
| `tokio` | Minimal stdio runtime for the MCP binary | `1.52.3`, exact, `default-features = false`; `io-std`, `io-util`, `macros`, `rt`, `sync` | Official [Tokio](https://tokio.rs/) project, crate metadata/source, and the concrete async stdio/runtime APIs used by the official SDK | Current locked release inspected 2026-07-18; actively maintained and cross-platform. The package already existed transitively; this direct edge fixes the minimal runtime contract | MIT | No networking, filesystem, process, multithread runtime, signal or time feature is enabled by this edge. It does not expand the MCP tool capability. |

The M7 delta grows the audited lock from 511 to 518 packages: one local `deskgraph-mcp` workspace package plus six registry packages (`rmcp 2.2.0`, `async-trait 0.1.89`, `futures 0.3.32`, `pastey 0.2.3`, `schemars_derive 1.2.1`, and `tokio-macros 2.7.1`). Their published license paths are Apache-2.0, MIT OR Apache-2.0, or MIT. The exact full lock was scanned against 1,160 cached RustSec advisories: zero vulnerabilities and the same 17 accepted pre-existing Tauri/Linux warnings; the cache-lock warning did not prevent the lockfile scan. No MCP SDK dependency receives a database writer, arbitrary path, filesystem action, network client, model, API key, Python, Docker, or Ollama capability. SQLite's official WAL documentation allows a read-only WAL connection when valid sidecars exist, can be created, or the database is immutable; the main database remains `READ_ONLY`/`query_only`, but adjacent WAL/SHM coordination sidecars may be created or updated. `immutable=1` is rejected because the Desktop writer may be active. Existing macOS/Linux sidecars are validated as ordinary non-links before/after open, and the exact bundled SQLite source independently uses `O_NOFOLLOW` for their SHM RW/RO opens and every VFS open including `SQLITE_OPEN_WAL`; the symlink-victim fixture stays unchanged. Other targets fail closed. In particular, the Windows VFS sidecar open cannot meet the same reparse/junction contract, so M7 returns unavailable there pending a safe VFS/snapshot design and native tests. SBOM/notices and remote-platform verification remain M9 gates.

## GitHub Actions

Only official `actions/*` actions are permitted in M0. `actions/checkout` v4.2.2 is pinned to `11bd71901bbe5b1630ceea73d27597364c9af683`; `actions/setup-node` v6.4.0 is pinned to `48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e`. CI permissions default to `contents: read`, and no secrets are exposed to fork pull requests. Remote execution evidence remains blocked until the GitHub repository exists.

## Executed verification

- Before the PDF dependency, `Cargo.lock` resolved 457 packages including eight DeskGraph workspace packages. `cargo metadata --offline --no-deps` found no missing workspace license metadata. License expressions include permissive licenses, MPL-2.0, and optional-license expressions containing LGPL; platform-specific redistribution and notices require another M9 review. PDF integration produced 483 packages; local workspace-only retrieval, benchmark, watcher, transaction, and project crates brought the lock to 488. Office integration produced 491 packages by adding `zip 8.6.0` (MIT), `typed-path 0.12.3` (MIT OR Apache-2.0), and `zlib-rs 0.6.6` (Zlib); `quick-xml 0.41.0` was already locked transitively and became an exact direct dependency. Image metadata produced 492 packages by adding only `imagesize 0.15.0` (MIT); target-specific macOS OCR brought that slice to 493 by adding only `objc2-vision 0.3.2` (Zlib OR Apache-2.0 OR MIT). The workspace-only OCR evaluator brought the lock to 494 through its local package row and already-locked `sha2 0.11.0`; the exact native-Watch target closure then adds 17 registry rows and brings the current lock to 511.
- `cargo tree --workspace --depth 1` recorded all direct versions. `cargo tree --target all -i ...` traced RustSec warnings to Tauri/Wry's Linux GTK3 stack and Tauri's URL-pattern parser chain.
- The current full-lock scan loaded 1,160 cached RustSec advisories and scanned 511 lockfile packages: zero known vulnerabilities plus 17 warnings—ten unmaintained GTK3 binding crates, `proc-macro-error`, five unmaintained `unic-*` crates, and one `glib` unsound advisory. The prior 492-, 493- and 494-package locks have the identical warning set; OCR, evaluator and exact native-Watch deltas add no advisory. The isolated 53-package PDF, 14-package Office, one-package image-metadata, and nine-package macOS binding closures separately returned zero findings. R-016 remains open because current evidence does not repair Tauri's Linux transitive stack.
- `pnpm audit --prod` and full `pnpm audit` found zero known vulnerabilities.
- `pnpm licenses list --json` failed with `ERR_PNPM_MISSING_PACKAGE_INDEX_FILE` under pnpm's SQLite-backed local store. The recorded equivalent scan read all installed package manifests: 145 unique packages, no missing license fields; 106 MIT, 18 Apache-2.0, 6 BSD-2-Clause, 2 BSD-3-Clause, 7 ISC, 2 MPL-2.0, 1 BlueOak-1.0.0, and 3 Apache-2.0 OR MIT.
- `pnpm peers check` reports no peer dependency issues after pinning TypeScript 6.0.3.

## Verification still required

- Execute frozen installs and all checks in remote macOS, Windows, and Linux CI.
- Revisit native transitive dependencies, redistribution notices, and R-016 when packaging begins.
- Re-run the Rust dependency and license audit after every future lockfile change.
- Audit every OCR, embedding, vector, model, and future archive/document dependency separately before adoption; rerun the full lock and license review after every accepted delta.

## Rejected or deferred at M0

- M0 intentionally had no database crate. M1 adopts the audited SQLite binding above; the health contract may report it ready only after initialization succeeds.
- No OCR, vector, embedding, or LLM runtime: they are not needed for the foundation slice.
- No Tauri filesystem, shell, HTTP, opener, or updater plugin: M0 health requires none of those capabilities.
- No telemetry/crash SDK: privacy and governance decision is unresolved.
