{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "M1 main window uses narrow Rust manifest commands with no filesystem, shell, network, or opener plugins.",
  "windows": ["main"],
  "permissions": []
}
