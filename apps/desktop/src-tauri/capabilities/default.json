{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "M0 main window capability with no filesystem, shell, network, or opener plugins.",
  "windows": ["main"],
  "permissions": []
}
