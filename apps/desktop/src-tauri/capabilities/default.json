{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "The main window has no frontend filesystem, dialog, shell, network, or opener permission; folder selection is owned by the narrow Rust authorization command.",
  "windows": ["main"],
  "permissions": []
}
